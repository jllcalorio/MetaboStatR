% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perform_SVM.R
\name{perform_SVM}
\alias{perform_SVM}
\title{Perform Support Vector Machine Classification on Preprocessed Metabolomics Data}
\usage{
perform_SVM(
  prepped_data,
  outcome_var = "Group",
  features = NULL,
  data_type = "NONPLS",
  use_merged = TRUE,
  kernel = "radial",
  tune_params = TRUE,
  cost = c(0.01, 0.1, 1, 10, 100),
  gamma = NULL,
  degree = c(2, 3, 4),
  cross = 10,
  probability = TRUE,
  seed = 42
)
}
\arguments{
\item{prepped_data}{Output from perform_PreprocessingPeakData function}

\item{outcome_var}{Character string specifying the outcome variable column name in metadata (default: "Group")}

\item{features}{Character vector of feature/metabolite names to include in analysis.
If NULL, uses all available features (default: NULL)}

\item{data_type}{Character string specifying which data matrix to use: "NONPLS" or "PLS" (default: "NONPLS")}

\item{use_merged}{Logical indicating whether to use merged replicates if available (default: TRUE)}

\item{kernel}{Character string specifying the SVM kernel: "linear", "radial", "polynomial",
or "sigmoid" (default: "radial")}

\item{tune_params}{Logical indicating whether to automatically tune cost and gamma via grid
search (default: TRUE)}

\item{cost}{Numeric or numeric vector of cost values to search when tune_params = TRUE,
or a single value to use directly (default: c(0.01, 0.1, 1, 10, 100))}

\item{gamma}{Numeric or numeric vector of gamma values to search when tune_params = TRUE
(only for radial, polynomial, sigmoid kernels), or a single value to use directly.
If NULL, defaults to 1/ncol(data) (default: NULL)}

\item{degree}{Integer or integer vector of degree values to search (only for polynomial kernel)
when tune_params = TRUE, or a single value to use directly (default: c(2, 3, 4))}

\item{cross}{Integer specifying number of cross-validation folds for tuning (default: 10)}

\item{probability}{Logical indicating whether to compute class probabilities (default: TRUE)}

\item{seed}{Integer for reproducibility (default: 42)}
}
\value{
Object of class "perform_SVM" (list) containing:
\itemize{
\item model: The final svm model object
\item accuracy: Overall cross-validated accuracy
\item confusion_matrix: Confusion matrix with class error rates
\item tuning_results: Output from tune() if tune_params = TRUE, else NULL
\item optimal_params: List of optimal cost, gamma, and/or degree used
\item metadata: Metadata used (QC samples removed)
\item data_matrix: Data matrix used
\item outcome_var: Name of outcome variable
\item features_used: Character vector of features used in the model
\item kernel: Kernel used
\item parameters: List of function call parameters
}
}
\description{
Perform Support Vector Machine Classification on Preprocessed Metabolomics Data
}
\examples{
svm_results <- perform_SVM(df_prepped, outcome_var = "Group")
svm_results <- perform_SVM(df_prepped, outcome_var = "Group",
                            kernel = "linear", tune_params = TRUE,
                            cost = c(0.1, 1, 10))
svm_results <- perform_SVM(df_prepped, outcome_var = "Group2",
                            features = c("Met001", "Met002", "Met010"))
}
