% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perform_RandomForest.R
\name{perform_RandomForest}
\alias{perform_RandomForest}
\title{Perform Random Forest Classification on Preprocessed Metabolomics Data}
\usage{
perform_RandomForest(
  prepped_data,
  outcome_var = "Group",
  features = NULL,
  data_type = "NONPLS",
  use_merged = TRUE,
  ntree = 1000,
  mtry = NULL,
  optimize_mtry = TRUE,
  mtry_range = 1:10,
  seed = 123,
  proximity = TRUE
)
}
\arguments{
\item{prepped_data}{Output from perform_PreprocessingPeakData function}

\item{outcome_var}{Character string specifying the outcome variable column name in metadata (default: "Group")}

\item{features}{Character vector of feature/metabolite names to include in analysis.
If NULL, uses all available features (default: NULL)}

\item{data_type}{Character string specifying which data matrix to use: "NONPLS" or "PLS" (default: "NONPLS")}

\item{use_merged}{Logical indicating whether to use merged replicates if available (default: TRUE)}

\item{ntree}{Integer specifying number of trees to build (default: 1000)}

\item{mtry}{Integer or NULL specifying number of variables to consider at each split.
If NULL, will optimize automatically (default: NULL)}

\item{optimize_mtry}{Logical indicating whether to optimize mtry parameter (default: TRUE)}

\item{mtry_range}{Integer vector specifying range of mtry values to test (default: 1:10)}

\item{seed}{Integer for reproducibility (default: 123)}

\item{proximity}{Logical indicating whether to compute proximity matrix (default: TRUE)}
}
\value{
List containing:
\itemize{
\item model: The final randomForest model object
\item oob_error: OOB error rate
\item confusion_matrix: Confusion matrix
\item error_trajectory: Data frame of error rates across trees
\item optimal_mtry: Optimal mtry value (if optimized)
\item mtry_optimization: Vector of OOB errors for different mtry values (if optimized)
\item metadata: Metadata used for the model
\item data_matrix: Data matrix used for the model
\item outcome_var: Name of outcome variable
\item features_used: Character vector of features used in the model
}
}
\description{
Perform Random Forest Classification on Preprocessed Metabolomics Data
}
\examples{
rf_results <- perform_RandomForest(df_prepped, outcome_var = "Group")
rf_results <- perform_RandomForest(df_prepped, outcome_var = "Group2",
                                    data_type = "PLS", ntree = 500)
# Use specific features only
rf_results <- perform_RandomForest(df_prepped,
                                    features = c("Met001", "Met002", "Met010"))
}
